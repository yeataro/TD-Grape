# UI 精簡與原生頁面整理 — 2026-09-12

版本維持 **0.8.5**。修改已寫入正式來源、更新運行中的 TD，並保存 `src/td/TD-Grape-dev.toe`。

## 完成

- 面板使用中性色，選中分頁無底線且高度減為 32px；Parameter 的節點底色標題同高並貼齊面板。
- Parameter 精簡重複說明與已連線輸入；增加選中連線後的斷開按鈕。畫布 Comment 直接顯示，統一文字色；編輯區仍可折疊。
- 同組分頁排序、跨組指定插入位置、垂直插入標記。Layout 選單提供面板顯示狀態、配置切換、保存、管理與重新命名，沿用 JSON 匯出入。
- Preview 的重新整理與背景改為圖示按鈕，保留可及性名稱、滑鼠提示及背景選中狀態，觸控裝置放大點按範圍。
- 窄版頂列依指定的兩列對齊；版本、副標及 About 在窄版隱藏。
- Source Subgraph 圖示 50% 亮度，明確提供本地可編輯副本入口；不更動來源定義。
- Label 轉為行尾註解；無獨立程式行／Subgraph 邊界保留單行標記。Comment 保持下方；除錯來源對照與單份編譯／顯示碼維持。
- MAT Buffer Output 可在 Settings 填寫辨識名稱；索引、連線及 GLSL 不受影響。
- Grape TOP／MAT 原生頁序、功能頁分組與分隔線完成；原有自訂控制及其綁定保留。新來源不自動建立外層來源頁。

## 驗證

- 137 項核心 Python 測試、14 項 Editor Launch 測試、語系鍵／品牌／定義與四組 JS 模型檢查通過。
- 瀏覽器：7 項既有 Layout 流程、6 組 UI 修訂流程、9 項節點身分流程、7 組 MAT buffer 操作、8 項預覽控制流程通過。另經 Tailscale 直連測試 744px 觸控模擬的圖示尺寸、背景切換與遠端 Viewer 隱藏；這不是 iPad 實機測試。
- TD：TOP／MAT 頁面整理、18 項自訂參數流程、6 項原生註解編譯／像素／巢狀錯誤定位通過。
- 現場只讀瀏覽器確認伺服器 UI 資源與來源相同；舊私人預覽入口亦提供新版樣式，仍不代表其已支援全部來源 API。
- 更新及保存均確認四份 Shader state 保留；排序另確認原值、預設值、Label、運算式與 Bind 表達式不變。TOE 保存時排除兩個私人工作元件。原始報告、截圖及舊 TOE 備份留在私人工作區。

本次驗證平台為 Windows／Chromium／TouchDesigner 2025.32820；macOS 與實體觸控裝置尚未實測。

## 留待後續

圖根恆常集合節點、完整 Sampler／Attributes／Constants／Buffers 來源管理、跨 stage 介面、MAT 預覽圖像來源與更多節點。Viewer 的 Window COMP 所有權、Label／來源資訊的畫布位置，以及 Buffer 名稱在 GLSL 的位置仍待決策。打包、安裝與公開發佈不在此輪範圍。

既有 Texture 的 Expose 控制尚待其來源／自訂控制遷移，不能將本輪描述成所有 Expose 已完全移除。現在的 shader 會於下一次套用使用新版 Label 格式；沒有批次重編譯使用者的圖。
